1. Install python and prolog and include Path to both
2.Open command prompt and type "pip install swiplserver"
3.Make sure that the python file and the prolog file are in the same directory
4.Run the python file in command line with "python <path/python file name>"